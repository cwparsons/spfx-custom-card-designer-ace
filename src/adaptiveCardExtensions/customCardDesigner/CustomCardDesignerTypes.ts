export enum CardIconSourceType {
  CustomImage = 1,
  Icon = 2
}

export enum CardImageSourceType {
  CustomImage = 1,
  Icon = 2
}

export enum CardTemplateType {
  Heading = 'basic',
  Image = 'image',
  Description = 'primaryText'
}
